{% extends "new_base.html" %}
{% import "bootstrap/wtf.html" as wtf %}
{% import "_macros.html" as macros %}
{%block nav%}
<li role="presentation"><a href={{url_for('main.daily')}}>日常</a></li>
<li role="presentation"><a href={{url_for('main.category',cat_id=0)}}>全部</a></li>
{% for cat in categories %}
	<li role="presentation"><a href={{url_for('main.category',cat_id=cat.id)}}>{{cat.name}}</a></li>
{% endfor %}
<li role="presentation"><a href={{url_for('main.about')}}>关于</a></li>
<li role="presentation" class="dropdown">
	<a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span></a>
	<ul class="dropdown-menu">
    	<li><a href={{url_for('main.new_blog')}}>新博客</a></li>
    	<li><a href={{url_for('main.new_cat')}}>新目录</a></li>
  	</ul>				
</li>
{%endblock%}


{% block body %}
{{super()}}
	{% if blogs %}
	{% for blog in blogs %}
	<section class="content-wrap">
		<div class="container">
			<div class="row">
				<main class="col-md-8 col-md-offset-2 main-content">
					<article id="2" class="post tag-https">
						<header class="post-head">
							<a href={{url_for('main.article',blog_id=blog.id)}}><h1 class="post-title">{{ blog.title }}</h1></a>
							<section class="post-meta">
							<p>{{ moment(blog.timestamp).fromNow(refresh=True) }}</p>
							</section>
						</header>
						<div style="padding-bottom:10px;">
							{%if single %}
								<p>摘要：{{ blog.abstract }}</p>
								<p>{{blog.text}}</p>
							{% else %}
								<p>{{ blog.abstract }}</p>
							{%endif%}
							<div class="row">
    							<div class="col-sm-1">
									<a href={{url_for('main.category',cat_id=blog.category.id)}}><p><span class="label label-info">{{ blog.category.name }}</span></p></div>
								<div class="col-sm-1" style="padding-left:15px;">
									<a href={{url_for('main.edit_blog',blog_id=blog.id)}} class="btn btn-default btn-xs" role="button">编辑</a>
								</div>
							</div>
						</div>
					</article>
				</main>
			</div>
		</div>
	</section>
	{% endfor %}
	{% if pagination %}
	<div class="pagination my-page-my">
		{{macros.pagination_widget(pagination,'main.category',cat_id=0)}}
	</div>
	{% endif %}
	{% endif %}
{% endblock %}


