{% extends "new_base.html" %}
{% import "bootstrap/wtf.html" as wtf %}
{% import "_macros.html" as macros %}

{% block head%}
{{super()}}
<style>
 .my-page-my{
 	display:block;
 }
</style>
{% endblock%}

{%block nav%}
<li role="presentation"><a href={{url_for('main.daily')}}>日常</a></li>
<li role="presentation"><a href={{url_for('main.category',cat_id=0)}}>博客</a></li>
<li role="presentation"><a href={{url_for('main.about')}}>关于</a></li>
<li role="presentation" class="dropdown">
	<a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span></a>
	<ul class="dropdown-menu">
    	<li><a href={{url_for('main.new_item')}}>新备忘</a></li>
    	<li><a href={{url_for('main.new_tag')}}>新标签</a></li>
  	</ul>		
</li>
<li>
	<div class="form-inline" style="padding-top:8px;padding-left:120px;">
	{{wtf.quick_form(form)}}
	</div>
</li>
{%endblock%}

{% block body %}
	{{super()}}
	{% if items %}
	{% for item in items %}
	<section class="content-wrap">
		<div class="container">
			<div class="row">
				<main class="col-md-8 col-md-offset-2 main-content">
					<article id="2" class="post tag-https">
						<header class="post-head">
							<a href={{url_for('main.item',item_id=item.id)}}><h1 class="post-title">{{ item.name }}</h1></a>
							<section class="post-meta">
							<p>{{ moment(item.timestamp).fromNow(refresh=True) }}</p>
							</section>
						</header>
						<div style="padding-bottom:10px;">
							<p>{{ item.text }}</p>
				   			<div class="row">
    							<div class="col-sm-1">
									<a href={{url_for('main.tag',tag_id=item.tag.id)}}><p><span class="label label-info">{{ item.tag.name }}</span></p></div>
								<div class="col-sm-1" style="padding-left:5px;">
									<a href={{url_for('main.edit_item',item_id=item.id)}} class="btn btn-default btn-xs" role="button">编辑</a>
								</div>
								<div class="col-sm-1" style="padding-left:0;">
									{% if item.flags %}
									<span class="glyphicon glyphicon-ok" aria-hidden="true" style="color:green"></span>
									{% else %}
									<span class="glyphicon glyphicon-remove" aria-hidden="true" style="color:red"></span>
									{% endif %}
								</div>
							</div>
						</div>
					</article>
				</main>
			</div>
		</div>
	</section>
	{% endfor %}
	{% if pagination %}
	<div class="pagination my-page-my">
		{{macros.pagination_widget(pagination,'main.daily')}}
	</div>
	{% endif %}
	{% endif %}
{% endblock %}

